Tasks required by stakeholder "-" defect/regression "+" enhancement

- STAKE-01 options flags not documented in help menu
- STAKE-02 opt_build_dir does not accept directory argument!
- STAKE-02a build file command should be changed from file -> build.
- STAKE-02b build file command must work for all patterns
- STAKE-02c build file should generate full project info in key=value lines
( build file features in 04_options.sh and 11_semver.sh and 01_config.sh )



- STAKE-03 syntax error line 4295/4297
- STAKE-04 semv should not allow version bumps outside of main branch

- STAKE-05 (BUILD PARTS SUPPORT) semv sync command for bash is really dumb, it needs to support
  checking in part files, or looking for the main script file that matches the
  name of the folder for example we are in folder prefix-name, and there is a file called
  name.sh, this is probably the main script file, unless there is a parts/ folder,
  then the 00_*.sh folder probably has version info (it can also be the FIRST file in build.map but the number may be different).
  If no bash files have version info the main 00_ part file is the most likely place to 
  add it, otherwise error and tell the user to pass the path to the file that needs version syncing.

+ STAKE-90 semv support information repos (document only no code)
